//_____ Base
export type competitionRequest = {
    // Define the properties for the competition request
};
export type competitionResponse = {
    // Define the properties for the competition response
};
export type leaderboardRequest = {
    // Define the properties for the leaderboard request
};
export type leaderboardResponse = {
    // Define the properties for the leaderboard response
};
export type parametersRequest = {
    // Define the properties for the parameters request
};
export type parametersResponse = {
    // Define the properties for the parameters response
};
//_____ Admin
export type getUsersRequest = {
    // Define the properties for the getUsers request
};
export type getUsersResponse = {
    // Define the properties for the getUsers response
};
export type manageUserRequest = {
    // Define the properties for the manageUser request
};
export type manageUserResponse = {
    // Define the properties for the manageUser response
};
export type manageCompetitionRequest = {
    // Define the properties for the manageCompetition request
};
export type manageCompetitionResponse = {
    // Define the properties for the manageCompetition response
};
//_____ User
export type signinRequest = {
    // Define the properties for the signin request
};
export type signinResponse = {
    // Define the properties for the signin response
};
export type loginRequest = {
    // Define the properties for the login request
};
export type loginResponse = {
    // Define the properties for the login response
};
export type autoLoginRequest = {
    // Define the properties for the autoLogin request
};
export type autoLoginResponse = {
    // Define the properties for the autoLogin response
};
export type updateBlockRequest = {
    // Define the properties for the updateBlock request
};
export type updateBlockResponse = {
    // Define the properties for the updateBlock response
};
//_____ Staff
export type rateSpecialBlockRequest = {
    // Define the properties for the rateSpecialBlock request
};
export type rateSpecialBlockResponse = {
    // Define the properties for the rateSpecialBlock response
};
